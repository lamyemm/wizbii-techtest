import React from 'react'
import { render, unmountComponentAtNode } from 'react-dom'
import { act } from 'react-dom/test-utils'
import Dashboard from './dashboard'

// jest.mock()

it('fetches posts and renders them on mount', () => {
  // TODO : mock fake data
})
